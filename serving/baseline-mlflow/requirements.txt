# Serving deps
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
mlflow==2.19.0
pandas==2.2.3
boto3==1.35.36

# Model flavor deps. Pinned to EXACT training-time version (verified by
# running `pip show transformers` on the training host, /home/cc/.local).
# This is the only version that has both required pieces:
#   - transformers.core_model_loading (added in 5.x, required by the pickle)
#   - "summarization" pipeline task (removed in the post-5.5.4 registry
#     refactor, so we can't just take any 5.x)
# If training ever moves to a newer transformers, update this to match.
# NOTE: torch is installed separately in the Dockerfile from the CPU-only
# wheel index to avoid pulling ~2GB of CUDA runtime we will never use on
# the k3s CPU node.
transformers==5.5.4
sentencepiece
