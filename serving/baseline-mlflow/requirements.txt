# Serving deps
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
mlflow==2.19.0
pandas==2.2.3
boto3==1.35.36

# Model flavor deps. NOTE: torch is installed separately in the Dockerfile
# from the CPU-only wheel index to avoid pulling ~2GB of CUDA runtime we
# will never use on a k3s CPU node.
transformers==4.45.2
sentencepiece==0.2.0
